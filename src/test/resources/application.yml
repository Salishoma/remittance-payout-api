seerbit:
  baseUrl: https://staging.seerbitapigateway.com/fcmb/api/v1
  grantType: client_credentials
  clientId: ${CLIENT_ID}
  clientSecret: ${CLIENT_SECRET}

server:
  servlet:
    context-path: /seerbit/api/v1
